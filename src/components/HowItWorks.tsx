export function HowItWorks() {
  return (
    <details className="how-it-works lean">
      <summary>How it works</summary>
      <p>
        We cross-check Jewel, Aldi &amp; Target — sale <em>and</em> everyday —
        so a store sale that loses to another store&apos;s shelf price never ranks
        as the best deal.
      </p>
    </details>
  );
}
