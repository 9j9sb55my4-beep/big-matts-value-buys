See README.md for full docs.

Live: Vite /api/flipp/search -> wishabi items/search; /api/flipp/flyer -> /flipp/flyers/{id} (no token). FlyerKit needs token. ZIP any US default 60610. History: seed + flipp-archive + flipp-live (scripts/backfill-history.mjs). Demo labeled fallback. Logo /big-matt-logo.png.

Flipp hosts current + some still-hosted older flyers; not a full year archive API. We ship a strong seeded+archived backline plus weekly append.
